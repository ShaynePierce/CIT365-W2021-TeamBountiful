@model MyMeetingManager.Models.Speaker

@{
    ViewData["Title"] = "Remove Speaker";
}

<h1 class="text-center">Remove Speaker</h1>
<hr />
<h3 style="color:red;">Are you sure you want to remove this Speaker?</h3>

<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Meeting)
        </dt>
        <dd class="col-sm-10">
            @{ if (Model.Meeting != null)
                {
                    @Model.Meeting.MeetingDate.ToShortDateString()
                } }
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Member)
        </dt>
        <dd class="col-sm-10">
            @{ if (Model.Member != null)
                {
                    @Model.Member.Salutation@: @Model.Member.Name
                } }
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Topic)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Topic)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Minutes)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Minutes)
        </dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="ID" />
        <input class="btn btn-danger" type="submit" value="Remove" /> |
        <a class="btn btn-info" asp-action="Index">Back to List</a>
    </form>
</div>
